#include <stdio.h>
int main () {

int fibo;


printf("Digite o primeiro número da fibo:");
scanf("%d",  &fibo);
printf("Digite o segundo número da fibo:");
scanf("%d",  &fibo);

for(int i=0;i<100;i++){
    if(fibo[i] = fibo + fibo)
        
    printf("%d", fibo[i]);
}
    
      
    return 0;
}