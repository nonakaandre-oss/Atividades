#include <stdio.h>
int main (){
    int num[8];
    int maior[];

    for(int cont=0;cont<8;cont++) {
        printf("Digite um numero inteiro: %d\n", cont);
        scanf("%d", &num[cont]);
    }

    while (maior = maior > num[8]);

    printf("o numero maior é %d\n:", maior);
    

    return 0;
}